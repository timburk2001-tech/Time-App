{
  "name": "time-app",
  "version": "1.0.0",
  "private": true,
  "description": "World Clock PWA — multi-timezone clock with time scrubbing",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
